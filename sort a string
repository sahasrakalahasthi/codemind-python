s=input()
rem = ''.join(ch for ch in s if ch.isalnum())
r=sorted(rem)
k=0
res=""
for i in range(0,len(s)):
    if s[i].isalnum():
        res+=r[k]
        k+=1
    else:
        res+=s[i]
print(res)
